@model WebHomestay.Models.Branch

@{
    ViewData["Title"] = "Thêm Chi nhánh";
}

<link rel="stylesheet" href="~/css/admin-premium.css" asp-append-version="true" />

<div class="premium-admin-body">
    <div class="admin-shell">
        <header class="admin-hero">
            <div class="admin-hero-title">
                <span class="admin-kicker">Cấu hình hệ thống</span>
                <h1>Khởi tạo Chi nhánh mới</h1>
            </div>
            <div class="admin-hero-actions">
                <a asp-action="Index" class="btn-premium-outline">
                    <i class="fas fa-arrow-left"></i>
                    <span>Quay lại</span>
                </a>
            </div>
        </header>

        <div class="admin-form-card">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger small mb-3"></div>
                
                <div class="row g-4">
                    <div class="col-md-7">
                        <div class="admin-form-group">
                            <label asp-for="Name" class="admin-form-label">Tên chi nhánh</label>
                            <input asp-for="Name" class="admin-form-input" placeholder="VD: StayEasy Quận 1" />
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>
                    </div>
                    <div class="col-md-5">
                        <div class="admin-form-group">
                            <label asp-for="Hotline" class="admin-form-label">SĐT Zalo</label>
                            <input asp-for="Hotline" class="admin-form-input" placeholder="090..." />
                            <span asp-validation-for="Hotline" class="text-danger small"></span>
                        </div>
                    </div>
                </div>

                <div class="admin-form-group">
                    <label asp-for="Email" class="admin-form-label">Email chi nhánh</label>
                    <input asp-for="Email" class="admin-form-input" placeholder="chinhanh@example.com" />
                    <span asp-validation-for="Email" class="text-danger small"></span>
                </div>

                <div class="admin-form-group">
                    <label asp-for="Address" class="admin-form-label">Địa chỉ chi tiết</label>
                    <input asp-for="Address" class="admin-form-input" placeholder="Số nhà, đường, phường, quận..." />
                    <span asp-validation-for="Address" class="text-danger small"></span>
                </div>

                <div class="admin-form-group">
                    <label asp-for="Description" class="admin-form-label">Mô tả & Giới thiệu</label>
                    <textarea asp-for="Description" class="admin-form-input" rows="4" placeholder="Thông tin nổi bật về chi nhánh..."></textarea>
                    <span asp-validation-for="Description" class="text-danger small"></span>
                </div>

                <div class="admin-form-group">
                    <label asp-for="MapUrl" class="admin-form-label">Google Maps URL</label>
                    <input asp-for="MapUrl" class="admin-form-input" placeholder="Dán link bản đồ vào đây" />
                    <span asp-validation-for="MapUrl" class="text-danger small"></span>
                </div>

                <div class="mt-5 pt-3 border-top d-flex gap-2">
                    <button type="submit" class="btn-premium px-5">
                        <i class="fas fa-save"></i>
                        <span>Lưu chi nhánh</span>
                    </button>
                    <a asp-action="Index" class="btn-premium-outline">Hủy bỏ</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
